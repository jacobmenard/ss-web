import { defineStore } from 'pinia';

export const useMatchStore = defineStore('match', {
    state: () => ({
        user: {}
    }),
    getters: {
        
    },
    actions: {
        async user() {
            
        }
    },
});
